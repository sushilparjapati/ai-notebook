PDF corpus (ML/DL textbooks) used by the CRAG notebooks in [`../code/`](../code/).

Referenced from notebooks as `../documents/bookN.pdf` (one level up from `crag/code/`).

Each RAG technique in this repo keeps its own `documents/` folder — see [`../../README.md`](../../README.md).